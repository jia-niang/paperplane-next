`mp-websocket-polyfill` 是一款适用于微信小程序的 Polyfill 库，它使微信小程序的 `WebSocket` 和浏览器端相兼容。

此工具也致力于对 [`Stomp.js`](https://github.com/stomp-js/stompjs) 的兼容；对于 iOS 端微信小程序上可能存在 `0x00` 字符等问题，工具也提供了修复。<br />
在 README 中，还给出了兼容 `TextEncoder`、`TextDecoder` 的兼容方案，为了包体积和灵活性的原因，此兼容方案未内置。
