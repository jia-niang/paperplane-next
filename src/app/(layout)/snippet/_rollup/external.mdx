此 `rollup.config.js` 配置文件适用于存在外部依赖项的场景。

例如：你正在为 Vue 开发组件库，而且需要导出 UMD 格式；UDM 格式没有模块化系统，代码中只能通过全局变量来访问 Vue 实例，所以代码必须把 Vue 配置为 “通过全局变量访问的外部依赖”。

配置如下：

```typescript
module.exports = [
  {
    // 添加下面这两行
    external: ['vue'],
    globals: { vue: 'Vue' },

    // 其它配置项 ...
  },
]
```
