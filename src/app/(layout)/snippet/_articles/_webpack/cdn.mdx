如果需要将静态资源存放在 CDN，可以使用以下配置。<br />
注意上传文件的流程需开发者自行处理，此配置仅修改项目中的资源 URL 前缀。

配置如下：

```js
module.exports = {
  output: {
    publicPath: `//cdn.example.com`,
  },
}
```

此处建议根据开发/生产环境进行区分，因为开发环境这段配置是不需要的。
