import { SnippetArticleCatelog } from '../list'
import settings from './settings.mdx'

export const vscode: SnippetArticleCatelog[] = [
  { key: 'settings', title: '用户设置 settings.json', component: settings },
]
